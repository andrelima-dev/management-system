{
  "name": "jungle-task-system",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "prettier --write .",
    "migration:run": "pnpm --filter @jungle/auth-service run migration:run && pnpm --filter @jungle/tasks-service run migration:run",
    "migration:revert": "pnpm --filter @jungle/tasks-service run migration:revert && pnpm --filter @jungle/auth-service run migration:revert"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "tsconfig-paths": "^4.2.0",
    "turbo": "^2.0.0",
    "typescript": "^5.4.5"
  }
}
